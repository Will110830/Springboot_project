
# Oracle DB 설정
spring:
  # app 이름
  application:
    name: project
  # DB 연결 정보
  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@localhost:1521:xe
    username: c##bbs
    password: bbs1234
  # JPA 설정
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
# 웹서버 포트
server:
  port: 9080

